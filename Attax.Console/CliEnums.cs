namespace Attax.Console {
    internal enum ProfileModeOption {
        Fixed,
        Random
    }

    internal enum SymmetryModeOption {
        None,
        Random,
        All
    }

    internal enum OrtOption {
        Cpu,
        Cuda
    }

    internal enum MirrorSideOption {
        Red,
        Blue
    }
}
